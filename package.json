{
  "name": "ui-troupe",
  "version": "0.0.4",
  "description": "🎭 UI Troupe is a minimal view-agnostic front-end application structure that leverages the actor model and statecharts in order to build predictable UIs",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "repository": {
    "url": "https://github.com/future-widget-lab/ui-troupe"
  },
  "scripts": {
    "build": "rimraf ./dist && tsc --version && tsc --project tsconfig.json",
    "development": "tsc --project tsconfig.json --watch",
    "start:example": "npm run build && parcel serve examples/todos/index.html"
  },
  "dependencies": {
    "@xstate/inspect": "0.5.2",
    "requestidlecallback-polyfill": "1.0.2",
    "xstate": "4.26.1"
  },
  "devDependencies": {
    "@emotion/css": "^11.7.1",
    "cuid": "^2.1.8",
    "lit-html": "^2.0.2",
    "parcel-bundler": "^1.12.5",
    "rimraf": "^3.0.2",
    "typescript": "^4.5.4"
  },
  "keywords": [
    "front-end",
    "xstate",
    "redux",
    "actor model",
    "state machines",
    "statecharts"
  ]
}
